<div class="content">
    <h1 style="text-align: center">Plagiarism Detection</h1>
    <br>
    {% if assignments != null and assignments|length > 0 %}
        <ul>
            {% for assignment in assignments %}
                <li>
                    <a href="{{ core.buildUrl({'component': 'admin', 'page': 'plagiarism', 'action': 'index', 'assignment': assignment}) }}">{{ assignment }}</a>
                </li>";
            {% endfor %}
        </ul>
    {% else %}
        <p>
            It looks like you have yet to run plagiarism detection on any assignment. See <a href="http://submitty.org/instructor/plagiarism">http://submitty.org/instructor/plagiarism</a> for details.
        </p>
    {% endif %}
</div>
