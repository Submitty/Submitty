{% include 'Vue.twig' with {
    type: 'component',
    name: 'RandomizeButtonWarning',
    args: {
        randomizeUrl: randomize_team_rotating_sections_url
    },
    events: {
        cancel: '() => { document.getElementById(\'randomize-button-warning\').style.display = \'none\'; }',
        confirm: '(url) => { location.href = url; }'
    }
} %}
