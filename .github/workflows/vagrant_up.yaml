name: Vagrant Up

on: 
  workflow_dispatch:
  schedule:
    - cron: '0 0 * * 0'

jobs:
  vagrant-up:
    runs-on: macos-10.15
    steps:
      - uses: actions/checkout@v2
      - name: Vagrant up
        run: |
          vagrant plugin install vagrant-vbguest
          vagrant up