{% import _self as self %}
<div class="content">
	<h2>Upload Site Wrapper [WIP]</h2>
	<br><br>
	<p>
        Instructors can upload three simple html files to use as a wrapper around the Submitty website.<br>
        The first will go in the upper left corner, the second across the top (upper right), and the third along the left side (lower left).<br>
        The uploaded HTML must be simple enough to not conflict with any of Submitty's HTML, or else the site may experience issues.<br>
        Do not include any scripts in the HTML.
    </p>
    <br>
    <p>
        Here is how the site will be laid out:
        <div class="wrapper-example">
            <div class="wrapper-top">Top Bar</div>
            <div class="wrapper-center">
                <div class="wrapper-left">Left<br>Sidebar</div>
                <div class="wrapper-body">Submitty</div>
                <div class="wrapper-right">Right<br>Sidebar</div>
            </div>
            <div class="wrapper-bottom">Bottom Bar</div>
        </div>
    </p>
	<br>
    <table class="wrapper-upload">
        <tr>
            <th>Panel</th>
            <th>Upload</th>
            <th>Current File</th>
        </tr>
        <tr class="wrapper-top">
            <td>Top Bar</td>
            <td>
                <form action="{{ core.buildUrl({'component': 'admin', 'page': 'wrapper', 'action': 'process_upload_html'}) }}" method="POST" enctype="multipart/form-data">
                    <input type="hidden" name="csrf_token" value="{{ core.getCsrfToken() }}" />
                    <input type="hidden" name="location" value="top_bar">
                    <input type="file" name="wrapper_upload">
                    <input type="submit" class="btn btn-default" value="Upload" />
                </form>
            </td>
            <td>
                {% if wrapper_files['top_bar'] != null %}
                    <a class="fa fa-trash" href="{{ core.buildUrl({'component': 'admin', 'page': 'wrapper', 'action': 'delete_uploaded_html', 'filename': 'upper-left'}) }}"></a>&nbsp;{{ wrapper_files['top_bar'] }}
                {% endif %}
            </td>
        </tr>
        <tr class="wrapper-left">
            <td>Left Sidebar</td>
            <td>
                <form action="{{ core.buildUrl({'component': 'admin', 'page': 'wrapper', 'action': 'process_upload_html'}) }}" method="POST" enctype="multipart/form-data">
                    <input type="hidden" name="csrf_token" value="{{ core.getCsrfToken() }}" />
                    <input type="hidden" name="location" value="left_sidebar">
                    <input type="file" name="wrapper_upload">
                    <input type="submit" class="btn btn-default" value="Upload" />
                </form>
            </td>
            <td>
                {% if wrapper_files['left_sidebar'] != null %}
                    <a class="fa fa-trash" href="{{ core.buildUrl({'component': 'admin', 'page': 'wrapper', 'action': 'delete_uploaded_html', 'filename': 'upper-right'}) }}"></a>&nbsp;{{ wrapper_files['left_sidebar'] }}
                {% endif %}
            </td>
        </tr>
        <tr class="wrapper-right">
            <td>Right Sidebar</td>
            <td>
                <form action="{{ core.buildUrl({'component': 'admin', 'page': 'wrapper', 'action': 'process_upload_html'}) }}" method="POST" enctype="multipart/form-data">
                    <input type="hidden" name="csrf_token" value="{{ core.getCsrfToken() }}" />
                    <input type="hidden" name="location" value="right_sidebar">
                    <input type="file" name="wrapper_upload">
                    <input type="submit" class="btn btn-default" value="Upload" />
                </form>
            </td>
            <td>
                {% if wrapper_files['right_sidebar'] != null %}
                    <a class="fa fa-trash" href="{{ core.buildUrl({'component': 'admin', 'page': 'wrapper', 'action': 'delete_uploaded_html', 'filename': 'lower-left'}) }}"></a>&nbsp;{{ wrapper_files['right_sidebar'] }}
                {% endif %}
            </td>
        </tr>
        <tr class="wrapper-bottom">
            <td>Bottom Bar</td>
            <td>
                <form action="{{ core.buildUrl({'component': 'admin', 'page': 'wrapper', 'action': 'process_upload_html'}) }}" method="POST" enctype="multipart/form-data">
                    <input type="hidden" name="csrf_token" value="{{ core.getCsrfToken() }}" />
                    <input type="hidden" name="location" value="bottom_bar">
                    <input type="file" name="wrapper_upload">
                    <input type="submit" class="btn btn-default" value="Upload" />
                </form>
            </td>
            <td>
                {% if wrapper_files['bottom_bar'] != null %}
                    <a class="fa fa-trash" href="{{ core.buildUrl({'component': 'admin', 'page': 'wrapper', 'action': 'delete_uploaded_html', 'filename': 'lower-left'}) }}"></a>&nbsp;{{ wrapper_files['bottom_bar'] }}
                {% endif %}
            </td>
        </tr>
        <tr class="wrapper-css">
            <td>Custom CSS</td>
            <td>
                <form action="{{ core.buildUrl({'component': 'admin', 'page': 'wrapper', 'action': 'process_upload_html'}) }}" method="POST" enctype="multipart/form-data">
                    <input type="hidden" name="csrf_token" value="{{ core.getCsrfToken() }}" />
                    <input type="hidden" name="location" value="css">
                    <input type="file" name="wrapper_upload">
                    <input type="submit" class="btn btn-default" value="Upload" />
                </form>
            </td>
            <td>
                {% if wrapper_files['css'] != null %}
                    <a class="fa fa-trash" href="{{ core.buildUrl({'component': 'admin', 'page': 'wrapper', 'action': 'delete_uploaded_html', 'filename': 'lower-left'}) }}"></a>&nbsp;{{ wrapper_files['css'] }}
                {% endif %}
            </td>
        </tr>
    </table>

</div>