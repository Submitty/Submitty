describe('Test course should exist', () => {
    it('Check test course exists', () => {
        cy.login();
        cy.visit(['development']);
        // Use this file to create a test based around submissions
        cy.get('jidsofjweiofjw');
    });
});
