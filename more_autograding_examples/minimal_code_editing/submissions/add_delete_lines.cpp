#include <string>
#include <iostream>

int main() {
      if (i == 0 || j == 0 || i == hw.size()+3 || j == hw.size()+3) {
        std::cout << '*';
      } else if (i == j && i > 1 && i < hw.size()+2) {
        std::cout << hw[i-2];
      } else {
        std::cout << ' ';
      }
    }
randomly 
insert some new lines
here
    std::cout << std::endl;
  }
}
