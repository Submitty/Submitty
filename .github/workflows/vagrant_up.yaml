name: Vagrant Up

on: 
  schedule:
    - cron: '0 0 * * 0'
  workflow_dispatch:
  push:

jobs:
  vagrant-up:
    runs-on: macos-10.15
    steps:
      - uses: actions/checkout@v2
      - name: Vagrant up
        run: |
          vagrant plugin install vagrant-vbguest
          vagrant up