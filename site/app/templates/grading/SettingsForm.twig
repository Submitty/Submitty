{% include 'Vue.twig' with {
    'type': 'component',
    'name': 'ta_grading/GradingSettings',
    'args': {
        'fullAccess': core.getUser().getGroup() <= 2
    },
    'events': {
        'setting-change': '(detail) => window.handleSettingChange(detail.storageCode, detail.value)',
        'hotkey-change': '(detail) => window.handleHotkeyChange(detail.index, detail.code)'
    }
} %}
